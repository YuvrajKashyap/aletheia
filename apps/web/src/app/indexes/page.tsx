import { SkeletonPage } from "@/components/layout/skeleton-page";

export default function IndexesPage() {
  return (
    <SkeletonPage
      title="Index Console"
      intent="Index Console will inspect active index metadata, OpenSearch status, and Qdrant status."
    />
  );
}
